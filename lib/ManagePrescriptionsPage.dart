import 'package:flutter/material.dart';

class ManagePrescriptionsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Manage Prescriptions'),
      ),
      body: Center(
        child: Text('Manage Prescriptions Page'),
      ),
    );
  }
}
